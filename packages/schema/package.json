{
  "name": "@formcraft/schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo \"(no lint yet — filled in Slice 1)\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(no tests yet — filled in Slice 1)\""
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
