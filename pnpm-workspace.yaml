packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks postinstall scripts unless they're named here. These are the
# native-binary downloads our toolchain genuinely needs; nothing else is
# allowed to run code at install time.
onlyBuiltDependencies:
  - esbuild
  - unrs-resolver
